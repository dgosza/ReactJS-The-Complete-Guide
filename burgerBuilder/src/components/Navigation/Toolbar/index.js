import React from 'react';
import './style.css'

import Logo from '../../Logo'

const Toolbar = (props) => {

    return(
        <header className="Toolbar">
            <div>Menu</div>
            <Logo/>
            <nav>
                ...
            </nav>

        </header>
    )

}

export default Toolbar